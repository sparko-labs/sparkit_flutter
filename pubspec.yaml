name: sparkit
description: A comprehensive Flutter utility package by SparkoSol, offering reusable widgets,
  mixins, and utilities for streamlined development.

version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
